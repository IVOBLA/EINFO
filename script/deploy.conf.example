#===============================================================================
# EINFO Deployment Konfiguration
#
# Kopiere diese Datei nach /home/bfkdo/deploy.conf und passe sie an:
#   cp deploy.conf.example /home/bfkdo/deploy.conf
#   nano /home/bfkdo/deploy.conf
#
# Das Deployment erfolgt immer in das Verzeichnis, aus dem das Skript
# gestartet wird (also das übergeordnete Verzeichnis von script/).
#===============================================================================

# Git Repository URL (HTTPS oder SSH)
# Für private Repos mit SSH: git@github.com:IVOBLA/EINFO.git
GIT_REPO_URL="https://github.com/IVOBLA/EINFO.git"

# Git Branch (main, develop, etc.)
GIT_BRANCH="main"

# Systemd Service Name für die Hauptanwendung
SERVICE_NAME="kanban-server"

# Systemd Service Name für den Chatbot (optional)
CHATBOT_SERVICE_NAME="chatbot"

# Server Port (falls kein systemd Service verwendet wird)
PORT="4000"
