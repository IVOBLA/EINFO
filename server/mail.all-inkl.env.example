# SMTP-Konfiguration für den Versand über All-Inkl (Kasserver)
#
# Hinweis: Ersetze alle Platzhalter (z.B. <deine-domain.de>) durch deine eigenen Werte.
# Die Zugangsdaten findest du im KAS unter "E-Mail" -> "Postfächer".

# Hostname des SMTP-Servers. Für All-Inkl entspricht er in der Regel deinem Servernamen
# (wXXXX.kasserver.com) oder dem Sammel-Relay smtprelaypool.ispgateway.de.
MAIL_HOST="smtprelaypool.ispgateway.de"

# Port 587 mit STARTTLS wird von All-Inkl empfohlen. SMTPS über Port 465 ist ebenfalls möglich,
# muss dann aber mit MAIL_SECURE=1 und MAIL_STARTTLS=0 genutzt werden.
MAIL_PORT=587
MAIL_SECURE=0
MAIL_STARTTLS=1

# Vollständige Mailadresse des Postfachs als Benutzername sowie das zugehörige Passwort.
MAIL_USER="postfach@deine-domain.de"
MAIL_PASSWORD="<starkes-passwort>"

# Absenderadresse, optional mit Namen. Fällt ohne Angabe auf MAIL_USER zurück.
MAIL_FROM="Feuerwehr Leitstelle <postfach@deine-domain.de>"

# Optional: Whitelist erlaubter Absenderadressen, falls zwischen mehreren identitäten gewechselt wird.
MAIL_ALLOWED_FROM="Feuerwehr Leitstelle <postfach@deine-domain.de>"
MAIL_DELETE_AFTER_READ=1  # 0 lässt Mails nach der Verarbeitung im Postfach und nutzt die .processed.json als Verlauf

# Optional: Ausführliches Logging der Mail-Aktivitäten aktivieren (Logdatei: logs/MAIL.log oder KANBAN_LOG_DIR).
#MAIL_LOG=1

# Optional: Abweichende Reply-To-Adresse.
#MAIL_REPLY_TO="leitstelle@deine-domain.de"

# Optional: Timeouts/TLS-Validierung je nach Umgebung anpassen.
#MAIL_TIMEOUT_MS=15000
#MAIL_TLS_REJECT_UNAUTHORIZED=1

# Optional: Eigener EHLO/HELO-Identifier (Standard ist der Hostname des Servers).
#MAIL_CLIENT_ID="leitstelle.de"
