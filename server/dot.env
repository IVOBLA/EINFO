# Allgemeine Server-Konfiguration
#KANBAN_LOG_DIR=./logs                    # Optionaler Ordner für Server-Logs (z.B. Log.txt, WMS_TILES.log); relativ zum Server-Root oder absolut.
# PORT=4000                               # HTTP-Port des Express-Servers (Default: 4000), über den das REST-Backend erreichbar ist und der mit Reverse-Proxy- bzw. Firewall-Regeln abgestimmt werden muss.
KANBAN_COOKIE_SECURE=0                  # "1" erzwingt Secure-Cookies für den Board-Login; aktivieren, wenn das Backend ausschließlich über HTTPS ausgeliefert wird.
BOARD_CACHE_MAX_AGE_MS=60000            # Maximale Cache-Dauer der berechneten Board-Daten in Millisekunden, bevor sie erneut aus dem Dateisystem geladen werden.
VEHICLE_CACHE_TTL_MS=10000              # Lebensdauer des Fahrzeug-Caches in Millisekunden, bevor Daten erneut geladen werden.
DATA_DIR=./data                         # Basisverzeichnis für alle persistenten Serverdaten (Einsatzlisten, Sessions, Druckdaten); relativer Pfad bezieht sich auf das Server-Working-Directory.
PUBLIC_DIR=./public                     # Optionales Verzeichnis für statische Dateien des WMS-Boards; bei Leerwert wird nur das API ausgeliefert.

# Frontend / UI Polling
UI_STATUS_POLL_INTERVAL_MS=3000         # Polling-Intervall (ms) für /api/ff/status und /api/ff/creds im Frontend.
UI_ACTIVITY_POLL_INTERVAL_MS=1000       # Polling-Intervall (ms) für /api/activity/status und zugehörige Laufzeitanzeigen.

# Feuerwehr-Feed (Fetcher & Läufer)
FF_OUT_FILE="./data/list_filtered.json"    # Ausgabedatei für den bereinigten Einsatz-Feed im JSON-Format, wird vom Frontend periodisch eingelesen.
FF_GPS_OUT_FILE="./data/vehicles_gps.json" # Ausgabedatei für GPS-Informationen der Fahrzeuge; dient dem WMS für Standortvisualisierungen.
FF_POLL_INTERVAL_MS=60000                  # Abrufintervall des Einsatz-Feeds in Millisekunden; geringere Werte erhöhen Last, höhere verlängern Aktualisierungszeiten.
FF_ACTIVITY_SWEEP_INTERVAL_MS=60000         # Prüfintervall (ms) für die automatische Überwachung der Fetcher-Aktivität und Auto-Stop-Logik.
FF_DEBUG=1                                  # "1" aktiviert detaillierte Log-Ausgaben des Fetchers inkl. HTTP-Status und Parsing-Informationen.
FF_LIST_PATH="/list"                       # Pfadanteil des Einsatzlisten-Endpunkts am Remote-Feed, wird an die Basis-URL aus der Konfiguration angehängt.
FF_LIST_EXTRA=""                           # Zusätzliche Query-Parameter für den Einsatzendpunkt, z.B. Filter oder Authentifizierungs-Token.
FF_LIST_TIMEOUT_MIN=2880                    # Maximale Zeit in Minuten, nach der ein nicht aktualisierter Feed als veraltet markiert und verworfen wird.
FF_GPS_PATH="/status/gps"                  # Pfadanteil für den Fahrzeug-GPS-Endpunkt, wird vom GPS-Fetcher verwendet.
FF_ONCE=0                                   # "1" lässt den Fetcher genau einen Abruf durchführen (Debug/Test); "0" hält den Dauermodus aktiv.
FF_CA_FILE=./feuerwehr_fullchain.pem        # Optionaler Pfad zur TLS-Zertifikatskette des Datenlieferanten für HTTPS-Verbindungen mit eigener CA.
FF_LOCK_FILE=""                            # Optionaler Dateipfad, der als Lock genutzt wird, um parallele Fetcher-Instanzen zu verhindern.
#FF_AUTO_STOP_MIN=30                         # Minuten bis zur automatischen Abschaltung eines Einsatzes ohne neue Ereignisse.
FF_USERNAME=""                             # Optionaler Benutzername für die HTTP-Basic-Authentifizierung oder API-Zugänge des Feeds.
FF_PASSWORD=""                             # Optionales Passwort bzw. API-Secret für den Feedzugang; nur in Kombination mit FF_USERNAME erforderlich.

# Umkreissuche (Near)
NEARBY_RADIUS_KM=1                         # Standardradius (Kilometer) für die Umkreissuche, wenn keine explizite Vorgabe im Client erfolgt.
NEARBY_RADIUS_MIN_KM=0.1                   # Kleinster zulässiger Radius in Kilometern, um punktuelle Suchanfragen zu begrenzen.
NEARBY_RADIUS_MAX_KM=50                    # Oberer Grenzwert des Radius, um unperformante Weitbereichssuchen zu unterbinden.

# Auto-Import & automatische Protokoll-Drucke
AUTO_IMPORT_DEFAULT_INTERVAL_SEC=30        # Standardintervall (Sekunden) für den automatischen Einsatz-Import, wenn keine Konfiguration vorliegt.
AUTO_PRINT_DEFAULT_INTERVAL_MINUTES=10     # Standardintervall (Minuten) für automatische Protokoll-Sammeldrucke.
AUTO_PRINT_MIN_INTERVAL_MINUTES=1          # Minimal erlaubtes Intervall (Minuten) für automatische Protokoll-Sammeldrucke.

# WMS / Lagekarte
WMS_PORT=8090                              # Port des internen WMS-Kartendienstes; muss frei sein und ggf. in Firewalls freigeschaltet werden.
WMS_TITLE="Lagekarte Board WMS"            # Titel, der in den WMS-Capabilities und im Kartenclient angezeigt wird.
WMS_ABSTRACT="Einsätze & Fahrzeuge wie MapModal"  # Längere Beschreibung des WMS-Angebots, erscheint in Metadaten/Capabilities.
WMS_LABELS=1                               # "1" blendet Kartenbeschriftungen ein; "0" reduziert die Karte auf reine Symbolik.
WMS_LABEL_FONT="12px Sans-Serif"           # CSS-Schriftangabe für Labels, beeinflusst Lesbarkeit und Layout.
WMS_LABEL_COLOR="#000000"                 # Hex-Farbwert der Beschriftungsschrift.
WMS_LABEL_OUTLINE="#ffffff"               # Hex-Farbwert des Label-Umrisses für bessere Kontraste.
WMS_LABEL_OUTLINE_W=3                     # Stärke des Outline-Strichs in Pixeln; größere Werte verbessern Lesbarkeit auf hellem Untergrund.
WMS_LABEL_TRIM=28                         # Maximale Zeichenlänge eines Labels, längere Texte werden abgeschnitten.
WMS_DEBUG=0                               # "1" aktiviert zusätzliche Debug-Informationen im WMS-Serverlog.

# Kanban & Druckausgabe (verwenden DATA_DIR als Basis)
KANBAN_MELDUNG_PRINT_DIR=./data/prints/meldung      # Zielverzeichnis für Meldungsdrucke aus dem Protokoll.
KANBAN_EINSATZ_PRINT_DIR=./data/prints/einsatz      # Zielverzeichnis für Einsatzdrucke (z. B. MapModal).
KANBAN_PROTOKOLL_PRINT_DIR=./data/prints/protokoll  # Ablage für automatische Protokoll-Sammeldrucke.
PUPPETEER_EXECUTABLE_PATH=""             # Optionaler Pfad zu einer alternativen Chrome-/Chromium-Binary, falls die Standardinstallation nicht genutzt werden soll.

# Benutzer-Sessions & Online-Status
USER_SESSION_IDLE_TIMEOUT_MIN=15          # Minuten der Inaktivität bis zum automatischen Logout; beeinflusst alle modernen Clients.
USER_SESSION_IDLE_TIMEOUT_MINUTES=15      # Alias für USER_SESSION_IDLE_TIMEOUT_MIN zur Abwärtskompatibilität älterer Konfigurationen.
SESSION_IDLE_TIMEOUT_MIN=15               # Historischer Alias für das Session-Timeout in Minuten, sollte konsistent zum Hauptwert bleiben.
SESSION_IDLE_TIMEOUT_MINUTES=15           # Weiterer Legacy-Alias für das Session-Timeout (Minuten) für Alt-Deployments.
USER_SESSION_IDLE_TIMEOUT_MS=900000       # Entsprechender Timeoutwert in Millisekunden, falls Umgebungen eine Millisekunden-Angabe erwarten.
USER_SESSION_SWEEP_INTERVAL_MS=60000      # Intervall in Millisekunden, in dem inaktive Sessions bereinigt werden.
# Hinweis: Online-Role-Aktivitätsfenster entspricht automatisch dem Session-Timeout (USER_SESSION_IDLE_TIMEOUT_MIN).

# Aufgaben / Fälligkeiten
DEFAULT_DUE_OFFSET_MINUTES=10             # Standardvorlauf in Minuten zwischen Aufgabenerstellung und Fälligkeitszeitpunkt.
TASK_DEFAULT_DUE_OFFSET_MINUTES=10        # Alias für DEFAULT_DUE_OFFSET_MINUTES, wird von älteren Task-Modulen gelesen.
AUFG_DEFAULT_DUE_MINUTES=10               # Deutscher Alias für DEFAULT_DUE_OFFSET_MINUTES in historischen Konfigurationen.

# Mailversand / SMTP
MAIL_HOST=""                             # Hostname oder IP des SMTP-Servers, der zum Versand genutzt wird.
MAIL_PORT=587                             # Port für die Verbindung; 587 für STARTTLS, 465 für SMTPS, 25 für unverschlüsselt.
MAIL_SECURE=0                             # "1" aktiviert SMTPS (TLS ab Verbindungsaufbau), "0" lässt unverschlüsselten Start zu.
MAIL_STARTTLS=1                           # "1" fordert STARTTLS nach der Verbindung an; bei reiner SMTPS-Nutzung auf "0" setzen.
MAIL_USER=""                             # Benutzername bzw. vollständige Mailadresse für die SMTP-Authentifizierung.
MAIL_PASSWORD=""                         # Passwort des SMTP-Accounts; wird nur übertragen, wenn Authentifizierung erforderlich ist.
MAIL_FROM=""                             # Absenderadresse für ausgehende Mails; ohne Angabe fällt der Wert auf MAIL_USER zurück.
MAIL_ALLOWED_FROM=""                     # Kommagetrennte Liste erlaubter Absenderadressen, optional mit Namen.
MAIL_LOG=0                               # "1" schreibt detaillierte Mail-Logs nach logs/MAIL.log (oder KANBAN_LOG_DIR), z.B. für Postfach-Analyse.
MAIL_DELETE_AFTER_READ=1                # "1" löscht Mails nach Verarbeitung; "0" belässt sie im Postfach und nutzt den Verlauf (.processed.json).
#MAIL_INBOX_POLL_INTERVAL_SEC=0            # Optionaler Abruf der Mail-Inbox in Sekunden; 0 oder leer deaktiviert das Polling.
#MAIL_INBOX_POLL_LIMIT=50                  # Maximale Anzahl an Mails pro Abruf; gelesene Dateien werden gelöscht.
#MAIL_IMAP_HOST=""                        # Hostname des IMAP-Servers für eingehende Mails (fällt ohne Angabe auf MAIL_HOST zurück).
#MAIL_IMAP_PORT=993                        # Port für IMAP (Standard 993 für TLS, 143 für StartTLS/Plain).
#MAIL_IMAP_SECURE=1                        # "1" nutzt TLS ab Verbindungsaufbau, "0" lässt unverschlüsselte Verbindung zu.
#MAIL_IMAP_USER=""                        # Benutzername für den IMAP-Login (fällt ohne Angabe auf MAIL_USER zurück).
#MAIL_IMAP_PASSWORD=""                    # Passwort für IMAP (fällt ohne Angabe auf MAIL_PASSWORD zurück).
#MAIL_IMAP_MAILBOX="INBOX"                # Name des Postfach-Ordners, der gelesen werden soll.
#MAIL_IMAP_TLS_REJECT_UNAUTHORIZED=1       # "1" lehnt selbstsignierte Zertifikate ab; bei eigener CA auf "0" setzen oder CA angeben.
#MAIL_POP3_HOST=""                        # Hostname des POP3-Servers für eingehende Mails (fällt ohne Angabe auf MAIL_HOST zurück).
#MAIL_POP3_PORT=995                        # Port für POP3 (995 für TLS, 110 für unverschlüsselt).
#MAIL_POP3_SECURE=1                        # "1" nutzt TLS ab Verbindungsaufbau, "0" lässt unverschlüsselte Verbindung zu.
#MAIL_POP3_USER=""                        # Benutzername für den POP3-Login (fällt ohne Angabe auf MAIL_USER zurück).
#MAIL_POP3_PASSWORD=""                    # Passwort für POP3 (fällt ohne Angabe auf MAIL_PASSWORD zurück).
#MAIL_POP3_TLS_REJECT_UNAUTHORIZED=1       # "1" lehnt selbstsignierte Zertifikate ab; bei eigener CA auf "0" setzen oder CA angeben.
#MAIL_REPLY_TO=""                         # Optional abweichende Antwortadresse; auskommentiert, solange nicht benötigt.
#MAIL_TIMEOUT_MS=15000                     # Optionales Timeout in Millisekunden für SMTP-Befehle und -Antworten.
#MAIL_TLS_REJECT_UNAUTHORIZED=1            # "1" lehnt selbstsignierte Zertifikate ab; bei eigener CA auf "0" setzen oder CA angeben.
#MAIL_CLIENT_ID=""                        # Optionaler EHLO/HELO-Identifier, überschreibt den Standard-Hostnamen des Servers.
EINFO_EXPERIMENTAL_SCENARIOPACK=0
